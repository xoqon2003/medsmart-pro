/**
 * DoctorPatientView — Used when a doctor navigates to a specific patient's record
 * from an external source (e.g., from a notification or deep link).
 * This simply renders DoctorDashboard which handles its own internal navigation.
 */
export { DoctorDashboard as DoctorPatientView } from './DoctorDashboard';
